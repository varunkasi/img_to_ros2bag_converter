# Dependencies for the Dockerized image_to_bag_converter
# These packages are installed in the Dockerfile

# Python packages
opencv-python>=4.5.0,<5.0.0
numpy>=1.20.0,<2.0.0

# ROS2 packages (installed via apt in the Dockerfile)
# ros-humble-cv-bridge
# ros-humble-rosbag2
# ros-humble-rosbag2-storage-mcap
# ros-humble-sensor-msgs

# Note: This file is maintained for documentation purposes.
# The actual installation of dependencies occurs in the Dockerfile.
